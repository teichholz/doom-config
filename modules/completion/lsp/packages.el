;; -*- no-byte-compile: t; -*-
;;; completion/lsp/packages.el

;; (when (featurep! :completion lsp)
;;   (package! lsp-mode)
;;   (package! lsp-ui)
;;   (package! company-lsp)
;;   (package! tide :disable t)

;;   (package! anaconda-mode :disable t)

;;   (when (featurep! +css)
;;     (package! lsp-css))

;;   (when (featurep! +cpp)
;;     (package! cquery)
;;     (package! rtags :disable t))

;;   (when (featurep! +sh)
;;     (package! company-shell :disable t)))
